<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ivan.ramos.biopacificv2">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BiopacificV2"
        android:usesCleartextTraffic="true">
        <activity android:name=".activitys.PdfViewResult" />
        <activity android:name=".activitys.PdfTarifario" />
        <activity android:name=".activitys.PdfViewFact" />
        <activity android:name=".activitys.ServiciosPorMedic" />
        <activity android:name=".activitys.ViewPager2" />
        <activity android:name=".activitys.Registro" />
        <activity android:name=".activitys.Login" />
        <activity android:name=".activitys.ServiciosPorMasc" />
        <activity android:name=".MainActivity" />
        <activity android:name=".activitys.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>